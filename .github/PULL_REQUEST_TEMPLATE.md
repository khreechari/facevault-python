<!-- Thanks for contributing! Please keep PRs to one topic. -->

## What & why

<!-- What does this change and why? Link any related issue. -->

## Checklist

- [ ] One topic per PR.
- [ ] Public API unchanged (`FaceVaultClient` / `AsyncFaceVaultClient` method signatures, model fields, exception types), or the break is called out above and justified.
- [ ] No new runtime dependencies added to `pyproject.toml`.
- [ ] `pytest` passes locally.
- [ ] `CHANGELOG.md` updated under `## [Unreleased]` (if user-facing).
