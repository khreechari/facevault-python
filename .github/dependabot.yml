version: 2
updates:
  # Keep the SHA-pinned GitHub Actions in CI / release workflows current.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  # Keep the SDK's runtime and dev dependencies current.
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
